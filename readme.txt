1: Place game files in "c:\dosbox\lostvik\"
2: Run data extractor
3: Run gamescreen viewer

This will decompress each chunk into raw binary files, written to game
folder.

Gamescreen viewer will load each gamescreen, dump its tiles, its metatiles,
and the tilemap.